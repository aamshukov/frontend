//..............................
// UI Lab Inc. Arthur Amshukov .
//..............................
#ifndef __TYPE_H__
#define __TYPE_H__

#pragma once

BEGIN_NAMESPACE(frontend)
USINGNAMESPACE(core)

template <typename R, typename P>
class type : public noncopyable
{
    //public:
    //    using return_type = R;
    //    using param_type = P;

    //    using hash_type = uint64_t;

    private:
        //flags       my_flags;
    //private:
    //    hash_type   hashcode();


    //public:
    //                type();
    //               ~type();

    //    bool        operator == (const type<return_type, param_type>& other);
    //    bool        operator != (const type<return_type, param_type>& other);

    //    return_type accept(const type_visitor_type<return_type, param_type>& visitor, param_type visitor_param);

};

END_NAMESPACE

#endif // __TYPE_H__
